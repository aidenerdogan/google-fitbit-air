# Docs

This folder contains shareable developer documentation only.

## Public Docs

- `ARCHITECTURE.md`: repository layout, ownership boundaries, and development flow.
- `XCODE_HEALTHKIT_SETUP.md`: how to open the iOS project and configure HealthKit locally.

## Private Docs

Private roadmap, review notes, and planning files live in `.project/` at the repository root.

`.project/` is intentionally ignored by Git.
