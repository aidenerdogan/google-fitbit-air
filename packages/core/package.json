{
  "name": "@health-passport/core",
  "version": "0.1.0",
  "description": "Open-core health continuity models, normalization, dedupe, and receipt logic.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "node --test --experimental-transform-types test/*.test.ts"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "license": "Apache-2.0"
}
